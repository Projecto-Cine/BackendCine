spring.application.name=demo

# Server - Render asigna su propio puerto via $PORT
server.port=${PORT:8080}

# Database - en Render setea DB_URL completa; localmente cae a localhost
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/cinema}
spring.datasource.username=${DB_USER:root}
spring.datasource.password=${DB_PASS:root}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# Cloudinary
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:dltofstl9}
cloudinary.api-key=${CLOUDINARY_API_KEY:657385785835193}
cloudinary.api-secret=${CLOUDINARY_API_SECRET}

# Email
spring.mail.host=${MAIL_HOST:smtp-relay.brevo.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:ab9c9e001@smtp-brevo.com}
spring.mail.password=${MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.ssl.trust=${MAIL_HOST:smtp-relay.brevo.com}
app.mail.from=${MAIL_FROM:equipo2lumencinema@gmail.com}

# JWT
jwt.secret=${JWT_SECRET:cinema-app-jwt-secret-key-2024-super-long-string-minimum-256-bits}
jwt.expiration=${JWT_EXPIRATION:86400000}

# Stripe
stripe.secret-key=${STRIPE_SECRET_KEY:sk_test_placeholder}
stripe.publishable-key=${STRIPE_PUBLISHABLE_KEY:pk_test_placeholder}
stripe.webhook-secret=${STRIPE_WEBHOOK_SECRET:whsec_placeholder}
